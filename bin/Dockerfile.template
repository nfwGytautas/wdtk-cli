#
# Template Dockerfile that build.py will use to generate Dockerfiles
#

# Generated at $timestamp

# Go as minimal as possible
FROM gcr.io/distroless/base-debian10

WORKDIR /

COPY ./$binDir$package /$package

EXPOSE 8080

USER root

ENTRYPOINT ["/$package"]
