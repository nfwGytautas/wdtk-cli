{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "[WDTK] Build",
            "type": "shell",
            "command": "go",
            "args": [
                "build",
                "-o",
                "wdtk",
                "*.go",
            ],
            "options": {
                "cwd": "${workspaceFolder}/cli/"
            }
        }
    ]
}
