apiVersion: v1
kind: PersistentVolume
metadata:
  name: coordinator-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 4Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/coordinator-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: coordinator-pv-claim
spec:
  storageClassName: manual
  resources:
    requests:
      storage: 4Gi
  accessModes:
    - ReadWriteOnce
