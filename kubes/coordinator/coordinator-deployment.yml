apiVersion: apps/v1
kind: Deployment
metadata:
  name: mstk-coordinator
spec:
  selector:
    matchLabels:
      app: mstk-coordinator
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mstk-coordinator
    spec:
      containers:
      - image: mstk/coordinator:0.0.0
        name: mstk-coordinator
        imagePullPolicy: Never
        resources:
          limits:
            memory: "500M"
            cpu: "50m"
        ports:
        - containerPort: 8080
          name: http
---
apiVersion: v1
kind: Service
metadata:
  name: mstk-coordinator
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: mstk-coordinator
